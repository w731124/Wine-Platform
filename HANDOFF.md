# HANDOFF

> 本檔案每次 session 結束時覆蓋重寫，不累加舊內容。最後更新：2026-09-08。

## 一、本次開發歷程

**本次session的主軸是延續上一版HANDOFF指定的任務：全面掃描quiz-data.js 300題題庫，找「沒有鑑別度的選項」與「用字通順度問題」。上一版HANDOFF（涵蓋至commit `0ed353c`）之後的所有工作皆屬本次，共6輪，commit範圍`955000a`～`8366678`，皆已push至`origin/main`。**

### 1. 第一輪：全題庫「全稱／唯一化斷言」型洩題干擾項（commit `955000a`、`762c30b`）
- 寫node腳本掃描「只有/只會/只能/只要/純粹是/絕對/毫無/唯一/永遠/完全不具備/從未/總是/所有…都/任何…都/完全沒有/必然/完全不/全面/一概/徹底」等把因果或範圍講死的極端斷言詞，統計全題庫這類用語共108處，僅4處在正解（且都是「否定/讓步」語法，性質不同），其餘全是干擾項——不需要葡萄酒知識，光憑「話講太滿通常是錯的」語感就有很高機率猜對，跟上個session的「其實/反而」洩題屬同一類問題。
- 涉及77題（含2題「不屬於XX代表產區」題型正解選項缺少地名後綴的格式洩題：`lo3-rie-007`、`lo3-syr-008`）全數改為語氣中性但知識點仍錯誤的敘述，只置換選項文字，不動correctIndex。DECISIONS.md #310-314含完整判準說明與部分逐題對照。
- 另外6處「兩者是完全不同的品種」（Pinot Grigio/Gris、Syrah/Shiraz、Albariño/Alvarinho同物異名考點）因錯誤性質需要實際品種知識才能判斷，第一輪刻意保留未處理，先列出回報使用者。

### 2. 第二輪：使用者截圖發現`lo3-cha-014`自我矛盾漏網題，擴大排查（commit `3022362`）
- 使用者截圖回報「Chardonnay為何被稱為全球適應力最強」這題，4選項中3個都直接違背題幹已預設的「適應力最強」前提（「全世界僅有單一產區」「只適合單一固定氣候」「完全不需要陽光與水分」），其中2個因未用到任何已掃描過的關鍵詞而漏網，1個是不需任何葡萄酒知識、純靠常識就能判斷不可能的選項。
- 依同一判準（題幹已用「聞名/著稱/公認」等詞明講品種招牌特性方向，選項卻直接主張相反方向）反向排查全題幹，找到21個含此類措辭的題目，逐題人工複核後另外抓到4題同類問題：`lo4-neb-004`、`lo3-cs-010`、`lo3-mer-014`、`lo4-zin-003`。全數改為維持題幹既定方向、但用錯誤的具體成因（需要實際知識才能排除）當干擾項。DECISIONS.md #315-317記錄完整過程，含教訓：純關鍵詞比對法有天生覆蓋率上限，須輔以「逐題整體檢視題幹與選項方向是否一致」的人工判斷。

### 3. 第三輪：處理第一輪保留的6處「同物異名」考點選項（commit `e045bc5`）
- 使用者確認處理方向：保留「這兩者其實不是同一品種」的錯誤方向與難度定位，但把可跨題重複記憶的「完全不同的品種」萬用句型，換成每題各自不同的具體（偽）技術性錯誤敘述（地理隔離演化、基因表現、克隆品系差異、雜交來源）。涉及`lo3-pgg-001`、`lo3-pgg-005`、`lo3-pgg-012`、`lo3-syr-006`、`lo4-alb-002`共5題6處。DECISIONS.md #318-319。

### 4. 第四輪：使用者截圖發現資料庫欄位名稱洩漏到題目文字，全面排查（commit `8366678`）
- 使用者截圖回報`lo3-pgg-006`題幹出現「confusionNote建議消費者...」，追問這是什麼意思。查明`confusionNote`是`data/wine-data.js`品種資料物件的實際欄位名稱，出題時忘記轉寫成中文就直接貼入題幹。
- 全面排查後確認共8個欄位名稱洩漏：`aromaWheel`(18)、`confusionNote`(11)、`representativeRegions`(8)、`servingTemp`(9)、`foodPairingTags`(8)、`skinColor`(8)、`originCountry`(8)、`styleSummary`(10)，合計73處，集中在LO3每個品種固定的「品種資料卡」子系列題目（第13-14題那批），研判是當初半自動轉寫時的系統性疏漏。其中9處在question欄位、64處在explanation欄位（已確認`js/quiz.js:630`會把explanation顯示給使用者，故一併處理，非僅question優先）。
- 依欄位語意換成對應中文敘述（`aromaWheel`→香氣輪、`confusionNote`/`styleSummary`→官方規格〔比照全題庫既有27處慣用寫法〕、`representativeRegions`→代表產區、`servingTemp`→建議侍酒溫度、`foodPairingTags`→建議搭餐項目、`skinColor`/`originCountry`裸露的`key:'value'`語法改寫成完整中文句子），不改變測驗知識點。DECISIONS.md #320-323。

### 5. 全部修正皆只動題目/選項/解析文字，不動correctIndex位置或題目結構
- 六輪修正中，只有第一輪的少數題目因整題選項重寫而需要留意（但正解仍在原index，未重排），其餘皆為原地文字置換。每輪修正後都執行完整收尾驗收：node腳本讀入300題陣列，VM沙盒解析語法、結構檢查（4選項/無重複）、id唯一性、correctIndex分佈（全程維持75/76/72/77均勻分佈）、no-run規則（連續3題以上同位置正解殘留數為0）、本session所有判準關鍵詞殘留數、駝峰式資料庫欄位名稱殘留數。

## 二、下一個session要做的事

**使用者尚未指定下一輪明確任務。** 本次「鑑別度」與「用字通順度」的原始任務範圍已完成，且過程中衍生處理了兩類使用者實測發現的新問題（題幹與選項方向矛盾的漏網題、資料庫欄位名稱洩漏），全部已修正並驗收。

若使用者於下個session要求「繼續稽核」，建議：
- 目前的稽核方法論（關鍵詞掃描找候選 + 人工逐條複核 + 抓到疑似案例先列出確認範圍）已證明多輪迭代後仍會有漏網案例（見第二輪、第四輪皆是使用者實測發現、非掃描腳本主動找到），提醒單純關鍵詞比對法有天生覆蓋率上限，若要進一步稽核建議搭配「使用者實際操作模擬考、逐題檢視」的方式效率較高，而非再對已掃描過的300題重複跑同樣的正則。
- `data/wine-data.js`本身尚未做過同等級的欄位名稱洩漏排查（本次只查了quiz-data.js是否洩漏wine-data.js的欄位名稱，未反向檢查wine-data.js自身內容或其他頁面元件是否有類似問題），若使用者之後在其他頁面（如品種百科、產區地圖等非模擬考頁面）也發現類似的「看到英文變數名稱」異狀，可比照本次做法（正則掃描駝峰式字串比對實際欄位名）處理。

## 三、我明確要求先記下來、之後再處理的內容

- 無新增。上一版HANDOFF的「時間趨勢折線圖」「跨session錯題本彙總」仍是使用者主動排除範圍的項目，非待辦事項。

## 四、現況檢查提醒

- **push狀態**：本次所有commit皆已push至`origin/main`，最新commit為`8366678`（本次HANDOFF.md覆寫將是下一個、也是最後一個commit）。
- **quiz-data.js目前狀態**：300題結構完整、id全數唯一、no-run規則全數通過、correctIndex維持75/76/72/77均勻分佈。已知的「自我矛盾干擾選項」「其實/反而語氣詞」（上個session）與「全稱/唯一化斷言干擾項」「題幹與選項方向矛盾」「資料庫欄位名稱洩漏」（本次session）等問題模式已清除乾淨。
- **`project-snapshot.md`目前仍為未追蹤的殘留檔案**（非本次session產生，上次「請打包目前網站」指令產生後未commit），如使用者確認不需要可以刪除，或留給下次打包指令覆蓋。
- **驗證方法**：本次全程用node腳本（VM沙盒解析+正則掃描）在scratchpad執行，純資料/文字內容判斷不需要開瀏覽器；若使用者回報「畫面上看到...」這類問題（如本次兩次截圖回報），優先確認是否為瀏覽器快取而非資料本身有誤（本次一次是真的資料問題、一次使用者誤以為是資料問題但其實是快取未更新，回報時有先用`git log -S`確認該處文字是否已在對應commit修正過）。
- **HANDOFF.md覆寫必須是該次session最後一個commit**——下一個session一開始應先讀本檔案，不需要使用者重新口述背景。
