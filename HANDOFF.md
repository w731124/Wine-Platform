# HANDOFF

> 本檔案每次 session 結束時覆蓋重寫，不累加舊內容。最後更新：2026-07-08。

## 一、本次開發歷程

延續產區↔品種雙向跳轉連結（`0802297`）之後，本次 session 執行**技術債清理**（使用者提供三筆已知技術債清單，要求逐筆核對現況後一次處理，並掃描是否有其他積存的技術債）。

### 三筆已知技術債的核對結果
1. **`auditWineDB()` 稽核錯誤欄位**——確認仍存在，已修正為讀取 `profile`（7維，含 `alcohol`／`finish`）而非 `radarStats`（5維），與 `compare.js` 實際渲染邏輯一致。
2. **`sensoryProfile`／`radarStats` 死碼殘留**——確認仍存在，且範圍隨本次 session 擴大到全部 **91** 筆產區（本 session 新增的36筆延續複製舊格式，一併帶入死欄位）。已用 `sed` 一次刪除182行（91筆×2欄位），刪除前備份、刪除後核對大括號/中括號配對與欄位數量。
3. **酒窖收藏功能殘留**——**確認已在更早的 session 完全移除**，此項可從技術債清單劃掉；唯一殘留的 `cleanupLegacyCellarStorage()` 是刻意保留的一次性 localStorage 清理函式，不是死碼。

### 額外掃出兩項
- **`compare.js` 的 `renderRadar()` 是零呼叫的孤兒函式**，已刪除。
- **地圖探索分頁的手繪 SVG 座標只覆蓋最早期約20個產區**，本次擴充的70個新產區（含全部6個新國家）完全沒有地圖標記。**判定為功能規模缺口、不是技術債**，已回報使用者但這次不處理，維持現狀。

以上已 commit（見下方「現況檢查提醒」），**尚未 push**。

## 二、討論過但尚未執行的項目

- **地圖探索分頁的座標覆蓋缺口**（見上方）——如果使用者之後想擴充地圖涵蓋新產區，需要手動設計新的 SVG 位置資料，屬於新任務，需另外確認範圍與工作量。
- 是否要 push 這次的 commit，尚待使用者決定。
- **沒有其他已知的下一步方向**——新 session 開場應直接詢問使用者接下來要做什麼。

## 三、我明確要求先記下來、之後再處理的內容

（本次 session 無新增此類項目——技術債清單已全數核對並處理完畢，除地圖座標缺口外無遺留項目。）

## 四、現況檢查提醒

- **本次 session 的異動（js/core.js／js/compare.js／data/wine-data.js）截至本檔案寫入時尚未 commit**，接手前請先確認原裝置是否已完成 commit。
- **資料完整性已核對**：移除死欄位後大括號/中括號配對正確（983/983、537/537），`wineColor`／`agingNote` 仍為91筆、id 無重複，用 headless Chrome `--dump-dom` 確認頁面仍可正常載入無錯誤。
- **`data/wine-data.js` 的每個產區物件現在只剩 `profile`（7維）一組感官數據欄位**，`sensoryProfile`（5維舊格式）與 `radarStats`（5維舊格式）已完全移除。**未來新增產區時，範本只需要 `profile` 欄位，不要再複製舊格式的 `sensoryProfile`／`radarStats`**——這正是本次技術債之所以擴大的原因，提醒接手者不要重蹈覆轍。
- **本機環境沒有 Node.js，Python 也無法執行**。
- **接手的 Claude Code 務必實際開啟異動的3個檔案核對真實現況**，不要只憑這份 HANDOFF.md 的文字描述去猜測。
